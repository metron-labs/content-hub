{
  "Name": "Update Incident",
  "Description": "Update Vega incident status, severity, verdict, assignee emails, and/or comment.",
  "Script": "from SiemplifyAction import SiemplifyAction\nfrom SiemplifyUtils import output_handler\nfrom ScriptResult import EXECUTION_STATE_COMPLETED, EXECUTION_STATE_FAILED\n\n\ndef _current_vega_id(siemplify):\n    try:\n        alert = siemplify.current_alert\n        events = getattr(alert, \"security_events\", None) or getattr(alert, \"events\", None) or []\n        for event in events:\n            additional = getattr(event, \"additional_properties\", None) or {}\n            value = additional.get(\"vega_id\") if isinstance(additional, dict) else None\n            if value:\n                return str(value)\n    except Exception:\n        return \"\"\n    return \"\"\n\n\n@output_handler\ndef main():\n    siemplify = SiemplifyAction()\n    from constants import (\n        INTEGRATION_NAME,\n        PARAM_ACCESS_KEY,\n        PARAM_ACCESS_KEY_ID,\n        PARAM_API_ROOT,\n        UPDATE_INCIDENT_SCRIPT_NAME,\n    )\n    from utils import format_user_facing_error, parse_csv_list, to_graphql_enum\n    from VegaManager import VegaManager\n\n    siemplify.script_name = UPDATE_INCIDENT_SCRIPT_NAME\n    incident_ids = parse_csv_list(\n        siemplify.extract_action_param(\"Incident IDs\", default_value=\"\")\n    )\n    if not incident_ids:\n        current = _current_vega_id(siemplify)\n        if current:\n            incident_ids = [current]\n    payload = {\"incidentIds\": incident_ids}\n    status = str(siemplify.extract_action_param(\"Status\", default_value=\"\") or \"\").strip()\n    severity = str(siemplify.extract_action_param(\"Severity\", default_value=\"\") or \"\").strip()\n    verdict = str(siemplify.extract_action_param(\"Verdict\", default_value=\"\") or \"\").strip()\n    reasoning = str(\n        siemplify.extract_action_param(\"Verdict Reasoning\", default_value=\"\") or \"\"\n    ).strip()\n    comment = str(siemplify.extract_action_param(\"Comment\", default_value=\"\") or \"\").strip()\n    emails = parse_csv_list(\n        siemplify.extract_action_param(\"Assignee Emails\", default_value=\"\")\n    )\n    if status:\n        payload[\"status\"] = to_graphql_enum(status)\n    if severity:\n        payload[\"severity\"] = severity\n    if verdict or reasoning:\n        payload[\"verdict\"] = {\"value\": verdict or \"NA\", \"reasoning\": reasoning}\n    if comment:\n        payload[\"comment\"] = comment\n    if emails:\n        payload[\"assigneeEmails\"] = emails\n    try:\n        if not incident_ids:\n            raise ValueError(\"Incident IDs are required.\")\n        manager = VegaManager(\n            api_root=siemplify.extract_configuration_param(INTEGRATION_NAME, PARAM_API_ROOT),\n            access_key_id=siemplify.extract_configuration_param(INTEGRATION_NAME, PARAM_ACCESS_KEY_ID),\n            access_key=siemplify.extract_configuration_param(INTEGRATION_NAME, PARAM_ACCESS_KEY),\n            logger_instance=siemplify.LOGGER,\n        )\n        result = manager.update_incidents(payload)\n        siemplify.result.add_result_json(result)\n        siemplify.end(\n            f\"Updated {len(incident_ids)} Vega incident(s).\",\n            True,\n            EXECUTION_STATE_COMPLETED,\n        )\n    except Exception as error:\n        message = f'Error executing action \"Update Incident\". Reason: {format_user_facing_error(error)}'\n        siemplify.LOGGER.error(message)\n        siemplify.end(message, False, EXECUTION_STATE_FAILED)\n\n\nif __name__ == \"__main__\":\n    main()\n",
  "IntegrationIdentifier": "Vega",
  "ScriptResultName": "ScriptResult",
  "DynamicResultsMetadata": [
    {
      "ResultName": "JsonResult",
      "ResultExample": "{}",
      "ShowResult": true
    }
  ],
  "Creator": null,
  "IsEnabled": true,
  "IsCustom": true,
  "AiGenerated": false,
  "IsSystem": false,
  "Version": 1.0,
  "TimeoutSeconds": 600,
  "IsAsync": false,
  "AsyncPollingIntervalInSeconds": 3600,
  "TotalIntervalTimeoutForAsyncInSeconds": 86400,
  "Parameters": [
    {
      "Name": "Incident IDs",
      "Type": 2,
      "IsMandatory": false,
      "Description": "Comma-separated Vega incident IDs. Optional when running on a Vega incident.",
      "DefaultValue": ""
    },
    {
      "Name": "Status",
      "Type": 2,
      "IsMandatory": false,
      "Description": "Possible Values: NEW, INVESTIGATING, ON HOLD, EXTERNAL ESCALATION, RESOLVED, REOPENED, REVIEW RECOMMENDED, RESPONSE REQUIRED, UNDER REVIEW.",
      "DefaultValue": ""
    },
    {
      "Name": "Severity",
      "Type": 2,
      "IsMandatory": false,
      "Description": "Possible Values: LOW, MEDIUM, HIGH, CRITICAL.",
      "DefaultValue": ""
    },
    {
      "Name": "Verdict",
      "Type": 2,
      "IsMandatory": false,
      "Description": "Possible Values: MALICIOUS, SUSPICIOUS, BENIGN, INCONCLUSIVE, NA.",
      "DefaultValue": ""
    },
    {
      "Name": "Verdict Reasoning",
      "Type": 2,
      "IsMandatory": false,
      "Description": "Verdict reasoning text.",
      "DefaultValue": ""
    },
    {
      "Name": "Comment",
      "Type": 2,
      "IsMandatory": false,
      "Description": "Comment to add on the Vega incident.",
      "DefaultValue": ""
    },
    {
      "Name": "Assignee Emails",
      "Type": 2,
      "IsMandatory": false,
      "Description": "Comma-separated assignee email addresses.",
      "DefaultValue": ""
    }
  ],
  "DefaultResultValue": "",
  "PythonVersion": "None",
  "ActionType": 1,
  "SimulationData": {
    "Entities": null
  },
  "SimulationDataJson": null,
  "AICategories": [],
  "OutcomeCategories": [],
  "AIDescription": null,
  "AiShortDescription": null,
  "ParametersDescription": null,
  "EntityTypes": []
}
